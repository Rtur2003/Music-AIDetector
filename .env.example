# Music AI Detector Configuration
# Copy this file to .env and customize as needed

# Data directory (where raw, processed, models, analysis folders are located)
# MUSIC_DETECTOR_DATA_DIR=backend/data

# Models directory (where trained models are stored)
# MUSIC_DETECTOR_MODELS_DIR=backend/data/models

# Temporary files directory (for vocal separation intermediate files)
# MUSIC_DETECTOR_TEMP_DIR=backend/temp

# Upload directory (for API file uploads)
# MUSIC_DETECTOR_UPLOADS_DIR=backend/uploads

# Audio processing sample rate (Hz)
# MUSIC_DETECTOR_SAMPLE_RATE=22050

# Demucs model name for vocal separation
# Options: htdemucs, htdemucs_ft, mdx_extra
# MUSIC_DETECTOR_DEMUCS_MODEL=htdemucs

# API Configuration (when running api.py)
# Maximum upload file size in MB
# MUSIC_API_MAX_UPLOAD_MB=25

# Maximum audio duration in seconds
# MUSIC_API_MAX_DURATION_SEC=600

# Maximum number of audio channels
# MUSIC_API_MAX_CHANNELS=2

# Rate limiting window in seconds
# MUSIC_API_RATE_WINDOW_SEC=60

# Maximum requests per rate limiting window
# MUSIC_API_RATE_MAX=30

# Vocal separation concurrency limit
# MUSIC_API_VOCAL_SEP_CONCURRENCY=2
